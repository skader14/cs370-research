package org.cloudbus.cloudsim.sdn.example;

/**
 * SimpleTopologyHelper stub.
 *
 * Note: The original attempt to implement a JSON-to-datacenter helper introduced
 * API mismatches with the project's types. We rely on the existing
 * SimpleExampleInterCloud utilities for topology parsing. This class remains as
 * a lightweight placeholder to avoid accidental imports elsewhere.
 */
public class SimpleTopologyHelper {
    // intentionally empty stub - use SimpleExampleInterCloud.createPhysicalTopology
}